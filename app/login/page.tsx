"use client";
import Link from "next/link";
import { useRouter } from "next/navigation";
import { ArrowRight } from "lucide-react";
import { DonnaMark } from "@/components/donna-mark";
import { Button } from "@/components/ui/button";

export default function LoginPage() { const router=useRouter(); const signIn=()=>{localStorage.setItem("donna-session","demo-user");router.push("/dashboard");}; return <main className="noise grid min-h-screen place-items-center bg-[#0a0a0c] p-6"><section className="w-full max-w-sm"><Link href="/" className="mb-10 flex items-center justify-center gap-3"><DonnaMark/><span className="font-semibold tracking-tight">Donna</span></Link><div className="rounded-2xl border border-white/[.09] bg-white/[.025] p-7 shadow-panel"><h1 className="text-2xl font-medium tracking-tight">Welcome back</h1><p className="mt-2 text-sm leading-6 text-zinc-500">Sign in to see what Donna has prepared for you.</p><form onSubmit={e=>{e.preventDefault();signIn();}} className="mt-7 space-y-4"><label className="block text-xs text-zinc-400">Email<input required type="email" placeholder="you@example.com" className="mt-2 h-11 w-full rounded-xl border border-white/[.09] bg-black/20 px-3 text-sm text-white outline-none placeholder:text-zinc-700 focus:border-violet-400/60"/></label><Button type="submit" className="h-11 w-full">Continue <ArrowRight size={16} className="ml-2"/></Button></form><div className="my-6 flex items-center gap-3 text-[10px] uppercase tracking-wider text-zinc-700"><span className="h-px flex-1 bg-white/[.07]"/>or<span className="h-px flex-1 bg-white/[.07]"/></div><Button onClick={signIn} variant="subtle" className="h-11 w-full">Continue with Google</Button></div><p className="mt-6 text-center text-xs text-zinc-600">Demo mode is active until Clerk keys are configured.</p></section></main>; }
