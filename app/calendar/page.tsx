"use client";
import { useState } from "react"; import { AppShell } from "@/components/app-shell"; import { PageHeading } from "@/components/page-heading";
const events=["SAT study block · 10:00 AM","Volunteer shift · Sunday 10:00 AM","Eagle Project planning · Thursday 4:00 PM"];
export default function CalendarPage(){const [view,setView]=useState("Month");return <AppShell><div className="mx-auto max-w-6xl p-5 sm:p-8"><PageHeading eyebrow="Planning" title="Calendar" description="Your agenda, with room to think." action={<div className="flex rounded-lg border border-white/[.08] p-1">{["Month","Week","Agenda"].map(x=><button onClick={()=>setView(x)} className={`rounded-md px-3 py-1.5 text-xs ${view===x?"bg-white/[.1] text-white":"text-zinc-500"}`} key={x}>{x}</button>)}</div>}/><section className="rounded-2xl border border-white/[.08] bg-white/[.025] p-5"><p className="mb-5 text-sm font-medium">{view} view · July 2026</p><div className="grid grid-cols-7 gap-2">{Array.from({length:35},(_,i)=><div key={i} className={`min-h-24 rounded-xl border p-2 text-xs ${i===14?"border-violet-400/50 bg-violet-500/[.08]":"border-white/[.06]"}`}><span className="text-zinc-600">{i+1}</span>{[9,14,20].includes(i)&&<p className="mt-3 truncate rounded bg-violet-400/15 px-1 py-0.5 text-[10px] text-violet-200">{events[[9,14,20].indexOf(i)].split(" · ")[0]}</p>}</div>)}</div></section><section className="mt-6 rounded-2xl border border-white/[.08] bg-white/[.025] p-5"><p className="text-sm font-medium">Today&apos;s agenda</p><div className="mt-4 space-y-3">{events.map(x=><div className="rounded-xl bg-white/[.04] p-3 text-sm text-zinc-300" key={x}>{x}</div>)}</div></section></div></AppShell>}
