"use client";
import { useState } from "react"; import { AppShell } from "@/components/app-shell"; import { PageHeading } from "@/components/page-heading";
export default function SettingsPage(){const [notifications,setNotifications]=useState(true);const [proactive,setProactive]=useState(true);return <AppShell><div className="mx-auto max-w-3xl p-5 sm:p-8"><PageHeading eyebrow="Workspace" title="Settings" description="Tune Donna to work the way you do."/><div className="space-y-4">{[["Profile","Nirav K. · Personal workspace"],["Security","Sign-in sessions are securely managed with Clerk when enabled."],["Theme","Dark mode is currently active."]].map(([a,b])=><section key={a} className="rounded-2xl border border-white/[.08] bg-white/[.025] p-5"><p className="text-sm font-medium">{a}</p><p className="mt-1 text-xs text-zinc-600">{b}</p></section>)}{[["Notifications",notifications,setNotifications],["AI preferences",proactive,setProactive]].map(([a,v,set])=><section key={String(a)} className="flex items-center justify-between rounded-2xl border border-white/[.08] bg-white/[.025] p-5"><div><p className="text-sm font-medium">{String(a)}</p><p className="mt-1 text-xs text-zinc-600">{String(a)==="Notifications"?"Receive timely reminders for important commitments.":"Let Donna proactively surface patterns and priorities."}</p></div><button onClick={()=>typeof set==="function"&&(set as (x:boolean)=>void)(!v)} className={`h-6 w-11 rounded-full p-1 ${v?"bg-violet-500":"bg-zinc-700"}`}><span className={`block h-4 w-4 rounded-full bg-white transition-transform ${v?"translate-x-5":""}`}/></button></section>)}</div></div></AppShell>}
