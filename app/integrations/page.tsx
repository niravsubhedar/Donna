"use client";
import { useState } from "react"; import { AppShell } from "@/components/app-shell"; import { PageHeading } from "@/components/page-heading";
const apps=["Google Calendar","Gmail","Google Drive","Google Tasks","Outlook","Slack","Discord","Zoom","Notion","GitHub"];
export default function IntegrationsPage(){const [connected,setConnected]=useState<string[]>([]);return <AppShell><div className="mx-auto max-w-6xl p-5 sm:p-8"><PageHeading eyebrow="Your tools" title="Integrations" description="Connect the services Donna needs to make your day run smoothly."/><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{apps.map(app=>{const yes=connected.includes(app);return <article key={app} className="rounded-2xl border border-white/[.08] bg-white/[.025] p-5"><div className="flex justify-between"><span className="grid h-9 w-9 place-items-center rounded-lg bg-violet-500/15 text-sm text-violet-200">{app[0]}</span><span className={`text-[10px] ${yes?"text-emerald-400":"text-zinc-600"}`}>{yes?"● Connected":"○ Not connected"}</span></div><p className="mt-4 text-sm font-medium">{app}</p><p className="mt-1 text-xs leading-5 text-zinc-600">Give Donna the right context while staying in control.</p><button onClick={()=>setConnected(yes?connected.filter(x=>x!==app):[...connected,app])} className="mt-5 rounded-lg border border-white/[.1] px-3 py-1.5 text-xs text-zinc-300 hover:bg-white/[.07]">{yes?"Disconnect":"Connect"}</button></article>})}</div></div></AppShell>}
