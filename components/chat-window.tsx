"use client";
import { ArrowUp, Sparkles } from "lucide-react";
import { useState } from "react";
export function ChatWindow() { const [message, setMessage] = useState(""); const [messages, setMessages] = useState<string[]>([]); function submit() { if (!message.trim()) return; setMessages([...messages, message]); setMessage(""); } return <section className="flex min-h-[420px] flex-col overflow-hidden rounded-2xl border border-white/[.09] bg-[#111115] shadow-panel"><div className="flex items-center gap-2 border-b border-white/[.07] px-5 py-4"><div className="grid h-7 w-7 place-items-center rounded-lg bg-gradient-to-br from-violet-400 to-indigo-600"><Sparkles size={14} /></div><div><p className="text-sm font-medium">Donna</p><p className="text-[10px] text-emerald-400">● Available</p></div></div><div className="flex-1 space-y-4 p-5"><div className="max-w-[92%] rounded-2xl rounded-tl-sm bg-white/[.07] px-4 py-3 text-[13px] leading-5 text-zinc-300">Good morning! I&apos;ve reviewed your day and found a few things that deserve your attention. Where would you like to start?</div>{messages.map((text, i) => <div key={i} className="ml-auto max-w-[85%] rounded-2xl rounded-tr-sm bg-violet-500 px-4 py-3 text-[13px] text-white">{text}</div>)}</div><form onSubmit={e => { e.preventDefault(); submit(); }} className="m-3 flex items-center gap-2 rounded-xl border border-white/[.08] bg-black/20 p-2 pl-3"><input value={message} onChange={e => setMessage(e.target.value)} placeholder="Ask Donna anything..." className="min-w-0 flex-1 bg-transparent text-xs text-zinc-200 outline-none placeholder:text-zinc-600" /><button type="submit" className="grid h-8 w-8 place-items-center rounded-lg bg-white text-zinc-900 transition-transform hover:scale-105"><ArrowUp size={15} /></button></form></section>; }
