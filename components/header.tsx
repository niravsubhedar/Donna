"use client";
import { Bell, Command, Menu, Search } from "lucide-react";
import { motion } from "framer-motion";
import { useState } from "react";
import { SearchDialog } from "@/components/search-dialog";
export function Header() { const [searchOpen,setSearchOpen]=useState(false); return <><header className="flex h-[76px] items-center justify-between border-b border-white/[.07] px-5 sm:px-8"><button className="text-zinc-400 lg:hidden"><Menu size={20} /></button><button onClick={()=>setSearchOpen(true)} className="hidden items-center gap-2 rounded-lg border border-white/[.07] bg-white/[.025] px-3 py-2 text-xs text-zinc-600 hover:border-white/[.15] md:flex"><Search size={15} /><span>Search anything...</span><kbd className="ml-20 flex items-center gap-0.5 rounded border border-white/[.07] px-1.5 py-0.5 text-[10px]"><Command size={9} /> K</kbd></button><div className="flex items-center gap-2"><motion.button whileTap={{ scale: .92 }} className="relative grid h-9 w-9 place-items-center rounded-lg text-zinc-500 hover:bg-white/[.06] hover:text-zinc-200"><Bell size={18} /><span className="absolute right-2 top-2 h-1.5 w-1.5 rounded-full bg-violet-400 ring-2 ring-[#0a0a0c]" /></motion.button><div className="ml-2 hidden h-8 w-px bg-white/[.07] sm:block" /><p className="hidden text-xs text-zinc-500 sm:block">Tuesday, July 15</p></div></header>{searchOpen && <SearchDialog onClose={()=>setSearchOpen(false)}/>}</>; }
